[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/init_db.py && flask db stamp head && gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
